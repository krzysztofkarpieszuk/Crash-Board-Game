export default class Pickup {
    constructor() {
        this.x = Math.floor(Math.random() * 10); // random position on X axis
        this.y = Math.floor(Math.random() * 10); // random position on Y axis
    }
}